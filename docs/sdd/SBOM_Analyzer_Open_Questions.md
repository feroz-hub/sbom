# SBOM Analyzer — Open Questions, Design Gaps and Stakeholder Decisions

| | |
|---|---|
| **Doc ID** | SBOM-DOC-002-OQ (companion to SBOM-DOC-002 Rev 0.1) |
| **Release Date** | 07-Jul-2026 |
| **Baseline** | Repository 2026-07-06/07 · backend 2.0.0 · Alembic head 041 |
| **Prepared By** | Feroze |

All unresolved items, missing evidence, design gaps and stakeholder decisions found during the SDD investigation. IDs OQ-001…OQ-026 are cited inline in the SDD. Severity guide in Impact column: P0 = must resolve before production with auth enabled; P1 = production-hardening; P2 = quality/documentation.

| ID | SDD Section | Question or Gap | Repository Evidence | Impact | Required Owner |
| --- | --- | --- | --- | --- | --- |
| OQ-001 | Front matter / 1.1 | Confirm official project code for the SBOM Analyzer | No project code found in repo | Document control | Project Manager |
| OQ-002 | Front matter | Obtain verbatim HCLTech copyright/confidentiality paragraph from CS-FRMK-DOC-002 | Reference document not in repo | Document control | Project Manager / Quality |
| OQ-003 | 2.1 | Locate/produce `SBOM-DOC-001` Requirement Specification; establish requirement-ID traceability | Not in repository; SDD traceability is to code/tests only | Regulatory traceability | Product Owner / Quality |
| OQ-004 | 4 | Record the SaaS-vs-self-hosted decision as an ADR (currently inferred from product shape) | No ADR covers it; `docs/adr/` = 0001, 0007, 0008, 0009 | Documentation (P2) | Architect |
| OQ-005 | 5.9 | Produce the consolidated five-phase flag rollout plan with metric gates (currently split across per-feature runbooks) | `docs/rollout-*.md`, `docs/runbook-*.md` | Operations (P2) | Architect / DevOps |
| OQ-006 | 5.10.6 | Incremental change feed: confirm whether still wanted; compare (ADR-0008) covers pairwise diffs only | Searched `changefeed/webhook/delta/since=` — none | Product scope | Product Owner |
| OQ-007 | 5.12.1 / 7.2 | Pin Node.js version (`engines` / `.nvmrc`); bootstrap installs Node 20 but nothing enforces it | No `engines` in `frontend/package.json`; no `.nvmrc` | Build reproducibility (P2) | Dev Team |
| OQ-008 | 7.1 | Define minimum server sizing (no load-test evidence beyond validation p95 benchmark) | `.benchmarks/` empty; `tests/validation/test_perf.py` only | Deployment planning | DevOps / Architect |
| OQ-009 | 7.3 | boto3 + `AWS_*` variables declared but no consumer found (`AWS_S3_BUCKET`/`AWS_S3_ENDPOINT_URL` appear dead) — remove or document intended S3 use | `.env.example`; `pyproject.toml:31`; grep `os.getenv("AWS` = 0 | Dead config (P2) | Dev Team |
| OQ-010 | 7.3 | `httpx2>=2.5.0` / `httpcore2` dev dependencies: non-standard forked package names, provenance undocumented; possible typo that may break `pip install .[dev]` | `pyproject.toml:51`; `requirements.txt` freeze | Supply-chain review (P1) | Dev Team / Security |
| OQ-011 | 8.1 / 10 | API prefix/versioning inconsistency (`/api`, `/api/v1`, `/dashboard`, `/admin/nvd-mirror`, `/analyze-sbom-*`) — adopt a target scheme and deprecation plan | `app/main.py:861-914`; API Inventory §1 | API governance (P2) | Architect |
| OQ-012 | 8.2.4 | VulnDB: no API key configured (adapter inert; every run logs a missing-key warning) and no live-API integration test — is the subscription planned? | `.env` has empty `VULNDB_API_KEY`; `vulndb.py:193-200` | Source coverage | Product Owner / Procurement |
| OQ-013 | 8.2.7 | Bundled 49 MB `data/xeol/1/xeol.db` appears unused (xeol_db provider disabled by default, no `XEOL_DB_PATH` set, no sync job/script) — activate or remove | `app/services/lifecycle/xeol_db_provider.py:29-58`; no sync script found | Repo hygiene / feature intent | Dev Team |
| OQ-014 | 9 | Produce the referenced security companions: Security Risk Management Plan, Threat Modelling, Security Risk Analysis | None of the three .docx/.xlsx exist in repo | Regulatory (P1) | Cybersecurity / Quality |
| OQ-015 | 10.1.3 | Notification channels (scan-complete/failed email/webhook/Slack): confirm requirement; nothing implemented beyond in-app toasts/SSE and schedule failure counters | Searched `webhook/smtp/notification` — none | Product scope | Product Owner |
| OQ-016 | 11 | No filesystem GC for expired `data/sbom-workspaces/` files (session rows carry `expires_at` but files are not swept) | `app/services/sbom/workspace_storage.py`; no sweep task | Disk growth (P1) | Dev Team |
| OQ-017 | 13 / 15 | No backup/restore runbook (only incidental "take a DB backup first" advice); forward-only migration 033 makes backups the sole rollback path | `docs/runbook-ai-credentials.md:48,216`; README roadmap row | Operations (P1) | DevOps |
| OQ-018 | 14.1 | Authorization-mapping defect: NVD-mirror admin router mounts at `/admin/nvd-mirror` but the permission map keys on `startswith("/api/nvd-mirror")` → falls through to generic `dashboard:read`/`tenant:settings:update` instead of `platform:admin`; admin-role split is a code TODO | `app/nvd_mirror/api.py:45-48`; `app/core/security.py:266` | **P1 security defect** | Dev Team / Security |
| OQ-019 | 14.6 | Define the canonical production auth combination: HCL IAM RBAC vs legacy `API_AUTH_MODE` (HS256/bearer) vs header-trusted `X-SBOM-Roles` gateway bridge — deployment guidance needed | `app/auth.py` vs `app/core/security.py`; stale comment `app/main.py:851-857` | Security clarity (P1) | Architect / Security |
| OQ-020 | 16.1 | Confirm IEC 62304 applicability/class for this program (platform is tooling, not device software) and Ed.2 status | IEC webstore shows Ed 1.1 (2015); Ed.2 status unknown | Regulatory | Quality / Regulatory |
| OQ-021 | 5.8 / 18 | `.env.example` omits `AI_CONFIG_ENCRYPTION_KEY` (present in real `.env`) and `NVD_MIRROR_FERNET_KEY` — fresh deployments will fail AI-credential and mirror-secret operations | `app/security/secrets.py:69`; `app/nvd_mirror/adapters/secrets.py:50` | Deployment (P1) | Dev Team |
| OQ-022 | 16.2 | CVSS v4.0 coverage on the live NVD path: mirror stores `score_v40` and VulnDB parses `cvss4`, but full v4 extraction in `_finding_from_raw` is Unable to Confirm — verify and document | `app/nvd_mirror/db/models.py:101`; `app/sources/vulndb.py:333-368` | Scoring accuracy | Dev Team |
| OQ-023 | 17.2 | `PARTIAL` semantics: code = "0 findings AND ≥1 provider error"; ADR-0001 prose reads broader ("some feed errored"). Confirm intended definition; today findings+errors ⇒ `FINDINGS` with only `query_error_count` and a " (partial)" label suffix as degradation signals | `app/services/analysis_service.py:139-150` vs `docs/adr/0001` | Metric semantics | Architect / Product Owner |
| OQ-024 | 17.4 | `match_strategy="cve_ids"` emitted by NVD but absent from the `MatchStrategy` Literal and `STRATEGY_FLOORS`; production NVD findings persist `match_confidence=NULL` — intended? | `app/sources/nvd.py:174`; `app/analysis.py:775-781`; `match_confidence.py:332-338` | Telemetry consistency (P2) | Dev Team |
| OQ-025 | 17.6 | `COMPARE_V1_FALLBACK` backend flag is dead (defined, docstring claims `/health` echoes it, no reader); FE uses its own `NEXT_PUBLIC_` twin — remove or wire | `app/settings.py:351-357`; grep = definition only | Dead flag (P2) | Dev Team |
| OQ-026 | 17.8 | Duplicate SBOM (name+version) on multipart `POST /api/sboms/upload` returns **500 internal_error** instead of 409 (no preflight; IntegrityError swallowed) — inconsistent with JSON create path | `app/routers/sbom_upload.py:344-350` vs `sboms_crud.py:711-722` | API defect (P1) | Dev Team |
| OQ-027 | 5.6 / 17.2 | **Zombie analysis runs permanently block re-analysis**: no age cutoff in `get_active_analysis_run`, no reaper; SSE client disconnect likely strands RUNNING (CancelledError not caught); recovery is manual SQL | `analysis_service.py:121,153`; `sboms_crud.py:1891` | **P1 operational defect** | Dev Team |
| OQ-028 | 5.10.1 | Stage-count drift: 9 actual validation stages vs "eight-stage" docstrings and 8-entry `STAGE_NUMBERS` (stage 9 serializes as `stage_number: 0`) | `app/validation/pipeline.py:66-96`; `stages/__init__.py:7-16` | Docs/serialization (P2) | Dev Team |
| OQ-029 | 5.10.5 | Run cancellation not implemented although FE label mapping for `CANCELLED` exists — confirm roadmap priority | `sboms_crud.py:175`; no cancel endpoint | Product scope | Product Owner |
| OQ-030 | 8.1 | **SSE authentication P0**: none of the three streaming clients sends JWT/tenant headers; with `AUTH_ENABLED=true` the analyze stream hard-fails and AI streams silently degrade to polling; also no unmount cleanup on the analyze stream | `useAnalysisStream.ts:125-144`; `AiBatchProgressProvider.tsx:156-216` | **P0 with auth on** | Dev Team (FE+BE) |
| OQ-031 | 8.2.2/8.2.3 | OSV/GHSA pipeline calls have no retries, rate limiting or circuit breaker (unlike NVD and the CVE-modal clients) — accept or harden | `app/analysis.py:1519-1548,2014-2035` | Resilience (P1) | Dev Team |
| OQ-032 | 8.2.3 | GHSA pagination is uncapped (`while hasNextPage`) — add a page cap consistent with NVD budgets | `app/analysis.py:1974-2003` | DoS/perf (P2) | Dev Team |
| OQ-033 | 5.7 | Celery ops hardening: no `task_acks_late`, no time limits, no prefetch/concurrency tuning, no dedicated queues; result backend = broker; beat crontabs hard-coded | `app/workers/celery_app.py`; audit BE-035 | Reliability (P1) | Dev Team / DevOps |
| OQ-034 | 5.10.3 | Schedule run-now handles SBOM and PROJECT scopes only; PRODUCT scope falls into 409 "Schedule is missing a target" | `app/routers/schedules.py:624-645` | Functional defect (P1) | Dev Team |
| OQ-035 | 8.2.1 | NVD per-scan caps (3×100 CVE batches, 10 CPE lookups, legacy `cpe_order[:10]`) silently truncate coverage on large SBOMs — no PARTIAL status or counter ties to truncation | `app/settings.py:49-50`; `app/analysis.py:2340` | Coverage transparency (P1) | Architect |
| OQ-036 | 5.6 / 11 | Startup runs unconditional full-table UPDATEs every boot (`analysis_run.sbom_name` sync; SQLite status reclassification) — slow boot at scale; multi-replica boot race | `app/main.py:361-362,624-638` | Scalability (P2) | Dev Team |
| OQ-037 | 10.1.1 | No dedicated audit-log test suite (writes asserted only incidentally); no PDF-content assertion test | searched `tests/test_audit*` — none | Verification gap (P2) | QA |
| OQ-038 | 11 | Nearly all domain timestamps are ISO-8601 String columns (`DateTime` only on identity/mirror tables); `_recent_run_exists` compares ISO strings — typed-datetime migration planned? | `app/models.py` passim; `scheduled_analysis.py:57-66` | Data quality (P2) | Architect |
| OQ-039 | 11 | Alembic 001 is `Base.metadata.create_all` (not a frozen snapshot) — fresh installs can mask missing migrations; deleted `e241e4c5d91e_tmp_frozen_snapshot` pyc suggests an abandoned freeze attempt; no CHECK constraints on run_status/severity vocabularies | `alembic/versions/001*`; `versions/__pycache__/e241*.pyc` | Migration integrity (P2) | Dev Team |
| OQ-040 | 5.12 / 15 | **No CI/CD pipeline in repo** (no `.github/`, GitLab, Jenkins); "fails CI"/"gate on every PR" claims are convention only; no dependency scanning (Dependabot/pip-audit/npm-audit); Railway builds whatever is pushed | searched all CI configs — none | **P1 process risk** | DevOps / Engineering Manager |
| OQ-041 | 8.3 | No browser E2E tests (Playwright/Cypress) and no visual-regression tooling; FE coverage stops at Vitest component/integration | frontend devDeps | Verification gap (P2) | QA |
| OQ-042 | 14 | No security headers/CSP anywhere (backend, Next config, or proxy — none ships); CORS default `*`; auth default off; `/docs`+`/openapi.json` public; `/health` leaks internal exception text; rate-limit key trusts first XFF hop | grep across app/frontend configs | Hardening (P1) | Dev Team / Security |
| OQ-043 | 14 | Redis unauthenticated/no TLS by default and absent from compose; broker compromise = task injection (JSON-only serializer limits blast radius) | `settings.py:225`; `docker-compose.yml` | Hardening (P1) | DevOps |
| OQ-044 | 9 | SSRF residual in VEX discovery: literal private IPs blocked but DNS-resolving-private hostnames pass and redirects are not re-validated per hop | `vex_discovery.py:229-257` (`follow_redirects=True` :93) | Hardening (P1) | Dev Team |
| OQ-045 | 5.10.2 | In-process idempotency cache and KEV/EPSS memos are per-process dicts — correctness across multiple uvicorn replicas unverified | `app/idempotency.py:25-40`; `kev.py:293-310` | Scale-out semantics (P2) | Architect |
| OQ-046 | 11 / 5.10.1 | Upload persistence is multi-commit (session → sbom → components) — a crash between commits leaves an SBOM without components (extraction-status flags mitigate); SBOM document metadata (tools/authors/timestamp/serialNumber) is extracted for NTIA but not persisted as columns | `sbom_upload.py:265-359`; `validation/normalize.py` | Data integrity (P2) | Dev Team |
| OQ-047 | 5.10.1 | Legacy XML extraction path (`xmltodict`/stdlib ET in `app/parsing/`) is not defusedxml-hardened — reachable for pre-validation legacy rows re-parsed via `extract_components` | `app/parsing/cyclonedx.py:105-198`; `format_detector.py:141-158` | Hardening (P2) | Dev Team |
| OQ-048 | 15 / repo hygiene | Working tree contains dev artefacts: real `.env` (gitignored), `sbom_api.db` (9.3 MB) + `.bak` (17 MB), 3 PPTX decks (~15 MB), `.coverage`, `graphify-out/`, stray root `node_modules/`, scratch files; README §19 cites stale head 035 (actual 041); metric-consistency allowlist comment says 11 files (actual 13); `backend-test-final.txt` is a truncated capture showing ≥1 failing test with no identification — latest full-suite status unknown | root listing; `tests/test_metric_consistency.py:781-797` | Hygiene / verification status | Dev Team / QA |
| OQ-049 | 8.1 | Frontend `.env.development` promised in code comments does not exist — fresh clones throw `NEXT_PUBLIC_API_URL is not configured` on `next dev`; auth.ts header comment contradicts stored refresh token | `api.ts:80`, `env.ts:31-33`, `auth.ts:12,142-144` | DX / docs (P2) | Dev Team |
| OQ-050 | 5.10.3 | Schedule resolver comments reference a partial-unique index on `analysis_schedule` not present in model table args — verify in migrations; ADR numbering gap 0002–0006 (skipped or external?) | `schedule_resolver.py` comments; `docs/adr/` | Docs (P2) | Dev Team |
| OQ-051 | 16 | Standards register externals needing confirmation: CISA BOD 22-01/KEV dates (cisa.gov unreachable to tooling), FDA Feb-2026 Federal Register citation, ISO/IEC 5962:2021 publication month, SPDX 2.3/3.0/3.0.1 exact dates, OWASP Top 10:2025 day, IEC 62304 Ed.2 status, GitHub GraphQL `securityVulnerabilities` parameter details | Evidence pack 10 | Regulatory citations | Quality / Regulatory |
